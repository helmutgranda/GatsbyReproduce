/*
 * Types
 */

/*
 * Initial State
 */
const initialState = {};

/*
 * Reducer
 */
export default (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

/*
 * Actions Creators
 */

/*
 * Selectors
 */
